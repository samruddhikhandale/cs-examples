{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "initializeCommand": "touch heythere.txt",
    "postCreateCommand": "bash ./sleep-me.sh && oryx build -p virtualenv_name=.venv --log-file /workspaces/.oryx/oryx-build.log --manifest-dir /workspaces/.oryx || echo 'Could not auto-build. Skipping.'"
}